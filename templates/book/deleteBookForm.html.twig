<form class="d-inline" method="post" action="{{ path('app_delete_book', {'user_id': app.user.id, 'book_id': book.id}) }}" onsubmit="return confirm('Are you sure you want to delete this book?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ book.id) }}">
	<button class="modif-button">


		
			<img src={{ asset ("../public/images/bin.png") }}>

		</button>
	</form>
