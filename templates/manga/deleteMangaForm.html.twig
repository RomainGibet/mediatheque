<form class="d-inline" method="post" action="{{ path('app_delete_manga', {'user_id': app.user.id, 'manga_id': manga.id}) }}" onsubmit="return confirm('Are you sure you want to delete this manga?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ manga.id) }}">
	<button class="modif-button">


		
			<img src={{ asset ("../public/images/bin.png") }}>

		</button>
	</form>
